%h1
  Users

%table{:class => 'index_table'}
  %tr
    %th
      Name
    %th
      Group
    %th
      Actions
  

  -for owner in @owners 
    %tr{:class => cycle('even','odd'), :id => "owner_#{owner.id}"}
      %td
        =link_to_remote owner.name, :url => owner, :method => :get, :update => 'container'
      %td
        =link_to_remote owner.group.name, :url => owner.group, :method => :get, :update => 'container'
      %td
        =link_to_remote 'Destroy', :url => owner, :confirm => 'Are you sure?', :method => :delete 
  
=link_to_remote 'New owner', :url => new_owner_path, :method => :get, :update => 'container'
